// const { withInfoPlist, ExportedConfig } = require('@expo/config-plugins');

// module.exports = function withLiveActivities(config) {
//   // 1. Add the mandatory Info.plist key
//   config = withInfoPlist(config, (config) => {
//     config.modResults.NSSupportsLiveActivities = true;
//     return config;
//   });

//   // 2. Here is where it gets complex: 
//   // You need to tell Xcode to create a "Widget Extension" target.
//   // Most developers use 'expo-apple-targets' to handle this part 
//   // because doing it manually in JS is 100+ lines of Ruby/XcodeProj code.
  
//   return config;
// };